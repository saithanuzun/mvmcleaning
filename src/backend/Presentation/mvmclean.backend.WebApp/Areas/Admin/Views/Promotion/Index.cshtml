@{
    ViewData["Title"] = "Promotion Management";
}

<div class="container-fluid mt-4">
    <div class="row mb-4">
        <div class="col">
            <h2 class="fw-bold">Promotion Management</h2>
            <p class="text-muted">Manage promotional campaigns and discounts</p>
        </div>
    </div>

    <div class="row g-3">
        <!-- All Promotions -->
        <div class="col-md-4">
            <a href="@Url.Action("AllPromotions", "Promotion", new { area = "Admin" })"
               class="btn btn-primary w-100 py-3">
                View All Promotions
            </a>
        </div>

        <!-- Create Promotion (placeholder for future) -->
        <div class="col-md-4">
            <a href="#"
               class="btn btn-secondary w-100 py-3" disabled title="Coming soon">
                Create Promotion
            </a>
        </div>
    </div>
</div>