@{
    ViewData["Title"] = "Invoice Management";
}

<div class="container-fluid mt-4">
    <div class="row mb-4">
        <div class="col">
            <h2 class="fw-bold">Invoice Management</h2>
            <p class="text-muted">Manage customer invoices and payments</p>
        </div>
    </div>

    <div class="row g-3">
        <!-- All Invoices -->
        <div class="col-md-4">
            <a href="@Url.Action("AllInvoices", "Invoice", new { area = "Admin" })"
               class="btn btn-primary w-100 py-3">
                View All Invoices
            </a>
        </div>

        <!-- Create Invoice (placeholder for future) -->
        <div class="col-md-4">
            <a href="#"
               class="btn btn-secondary w-100 py-3" disabled title="Coming soon">
                Create Invoice
            </a>
        </div>
    </div>
</div>