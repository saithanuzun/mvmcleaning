@{
    ViewData["Title"] = "Invoice Details";
}

<div class="container mt-4">
    <div class="row mb-4">
        <div class="col-md-12">
            <h2 class="mb-4">
                <i class="bi bi-receipt"></i> Invoice Details
            </h2>
            <a href="@Url.Action("Index", "Invoices")" class="btn btn-secondary mb-3">
                <i class="bi bi-arrow-left"></i> Back to Invoices
            </a>
        </div>
    </div>

    <div class="alert alert-info" role="alert">
        <i class="bi bi-info-circle"></i> Invoice details feature coming soon.
    </div>

    <div class="card shadow-sm">
        <div class="card-body">
            <p class="text-muted">
                Invoice details will be displayed here once the invoicing system is implemented.
            </p>
        </div>
    </div>
</div>
